function startApp() {
    $('#show-gen-qr').hide();
    $('#show-scan-qr').hide();
}

// Launching the function when the page loads
window.addEventListener('load', async function () {
    startApp();
})